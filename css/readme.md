# How to manually compile CSS

In the root directory of this project run this command:
`npx tailwindcss build css/main.css -o css/output.css`

# `npm run compile` in the root directory also compiles the css